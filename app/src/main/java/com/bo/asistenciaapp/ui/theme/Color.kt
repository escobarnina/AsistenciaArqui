package com.bo.asistenciaapp.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de colores para la aplicación AsistenciaApp.
 * 
 * Esta paleta está diseñada para una aplicación educativa/académica,
 * utilizando colores que transmiten profesionalismo, confianza y claridad.
 * 
 * Esquema de colores:
 * - Primary: Azul académico (confianza, profesionalismo)
 * - Secondary: Verde (éxito, crecimiento)
 * - Tertiary: Naranja (atención, acción)
 * - Error: Rojo (alertas, errores)
 * - Success: Verde (operaciones exitosas)
 */

// Colores primarios (Azul académico)
val Blue80 = Color(0xFF9BB5FF)      // Primary para tema oscuro
val Blue40 = Color(0xFF2563EB)     // Primary para tema claro (Azul profesional)

// Colores secundarios (Verde)
val Green80 = Color(0xFF81D4A3)    // Secondary para tema oscuro
val Green40 = Color(0xFF10B981)    // Secondary para tema claro (Verde éxito)

// Colores terciarios (Naranja)
val Orange80 = Color(0xFFFFB88C)   // Tertiary para tema oscuro
val Orange40 = Color(0xFFF97316)   // Tertiary para tema claro (Naranja acción)

// Colores de estado
val Error80 = Color(0xFFF87171)    // Error para tema oscuro
val Error40 = Color(0xFFEF4444)     // Error para tema claro

val Success80 = Color(0xFF6EE7B7)  // Success para tema oscuro
val Success40 = Color(0xFF10B981)  // Success para tema claro

// Colores neutros
val Neutral80 = Color(0xFFE5E7EB)  // Neutral para tema oscuro
val Neutral40 = Color(0xFF6B7280)  // Neutral para tema claro

// Colores de superficie
val SurfaceDark = Color(0xFF1E293B)    // Superficie para tema oscuro
val SurfaceLight = Color(0xFFFFFFFF)   // Superficie para tema claro

// Colores de fondo
val BackgroundDark = Color(0xFF0F172A)  // Fondo para tema oscuro
val BackgroundLight = Color(0xFFF8FAFC) // Fondo para tema claro

// Colores de texto
val OnPrimaryDark = Color(0xFF0F172A)   // Texto sobre primary en tema oscuro
val OnPrimaryLight = Color(0xFFFFFFFF)  // Texto sobre primary en tema claro

val OnSurfaceDark = Color(0xFFF1F5F9)   // Texto sobre superficie en tema oscuro
val OnSurfaceLight = Color(0xFF1E293B)  // Texto sobre superficie en tema claro